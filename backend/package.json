{
  "name": "backend",
  "version": "1.0.0",
  "description": "1. Rename `.env.example` to `.env` 2. Fill in your PostgreSQL credentials:    - DB_USER=    - DB_HOST=    - DB_NAME=    - DB_PASSWORD=    - DB_PORT=5432",
  "main": "db.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^17.3.1",
    "express": "^5.2.1",
    "pg": "^8.20.0",
    "ssh2": "^1.17.0"
  }
}

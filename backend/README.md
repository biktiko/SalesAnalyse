# Backend for Martin Star Analytics

## Database Connection
1. Rename `.env.example` to `.env`
2. Fill in your PostgreSQL credentials:
   - DB_USER=
   - DB_HOST=
   - DB_NAME=
   - DB_PASSWORD=
   - DB_PORT=5432

## Development
```bash
npm install
node server.js
```
